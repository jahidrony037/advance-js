// Undefined vs Null 
// undefined
let pakhi;

function add(num1, num2) {
    num1 + num2 //if you don't return then it is undefined
}

function add(x, y) {
    x + y;
}

const premik = { name: "kalimoddi", address: "kata-bon" };

console.log(premik.gfName);

let fun = undefined;
console.log(fun);

let ages = [2, 5, 9];
console.log(ages[11]);

//null set kore dite hoy but undefined onk vabei paoa jay jemon kono variable er value set kore  na dile othoba kono array or object thake kono properties access kora jeta oi array or object a exitence kore na