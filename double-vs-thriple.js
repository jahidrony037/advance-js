const first = false;
const second = 0;
if (first === second) { //double == check only value 
    //thriple === check value and data type both
    console.log('condition is true');
} else {
    console.log('condition is false');
}